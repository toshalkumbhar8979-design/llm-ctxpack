[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "llm-ctxpack"
version = "0.2.1"
description = "Pack the most important text into a fixed LLM context-window token budget."
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }
authors = [{ name = "llm-ctxpack contributors" }]
keywords = ["llm", "ai", "context-window", "rag", "tokens", "prompt-engineering"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Text Processing :: Linguistic",
]
dependencies = []

[project.optional-dependencies]
tiktoken = ["tiktoken>=0.5"]
dev = ["pytest>=7"]

[project.urls]
Homepage = "https://github.com/toshalkumbhar8979-design/ctxpack"

[project.scripts]
ctxpack = "llm_ctxpack.cli:main"

[tool.setuptools.packages.find]
include = ["llm_ctxpack*"]